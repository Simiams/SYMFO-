{% extends 'base.html.twig' %}

{% block title %}Modify the PAddock{% endblock %}

{% block body %}
    {% for message in app.flashes('success') %}
        <div class="alert alert-danger">
            {{ message }}
        </div>
    {% endfor %}
    <div class="example-wrapper">
        <h1>Modify the paddock <i>{{ paddock.name }}</i></h1>
        {{ form(formular) }}
    </div>
{% endblock %}
